# JF
 
An interpreter for the BF programming language written in the Julia programming language

Not fully functional yet, since the input command is not implemented yet and the run file currently just runs the testscript in ascii mode.
